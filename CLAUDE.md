# Pension_Management_System

- No manifest file exists at repo root (no package.json/go.mod/pyproject.toml/Cargo.toml) — no build/test/lint commands to run; none found.
- Repo currently contains only planning/design artifacts, not source code — do not assume an ASP.NET Core project exists on disk.
- `pension Management.pen` is an encrypted Pencil design file — never open with Read/Grep, only Pencil MCP tools.
- Filesystem here is case-insensitive (APFS) — `ARCHITECTURE.md` and `architecture.md` resolve to the same file; be careful when creating similarly-named files.

Files worth reading first:
- `README.md` — project overview, planned tech stack, planned folder structure
- `architecture.md` — long-form (not-yet-implemented) architecture write-up
- `ARCHITECTURE.md` — current, accurate summary of what's actually on disk

Architecture: see ARCHITECTURE.md — read before structural changes
